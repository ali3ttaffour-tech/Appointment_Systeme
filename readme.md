# Appointment System – Spring Boot

## 📌 شرح عام عن المشروع
مشروع **Appointment System** هو تطبيق Backend مبني باستخدام **Spring Boot** يهدف إلى إدارة نظام حجز مواعيد بين المستخدمين والخدمات.

يوفر النظام إمكانيات:
- إنشاء حساب وتسجيل دخول المستخدمين
- إدارة الخدمات (Services)
- حجز المواعيد (Appointments)
- التحقق من توفر الوقت
- التعامل مع قاعدة البيانات باستخدام JPA و Hibernate
- بناء RESTful APIs لاستخدامها من قبل تطبيق ويب أو موبايل

التقنيات المستخدمة:
- Java 17
- Spring Boot
- Spring Web
- Spring Data JPA
- Spring Security (إن وُجد)
- H2 / MySQL
- Maven
- Lombok

---

👥 تقسيم المشروع بين الطلاب

تم تنفيذ هذا المشروع بأسلوب شبه جماعي، حيث قام كل طالب بالعمل على نسخة مستقلة من المشروع في البداية، ثم تمت مقارنة الأعمال واختيار أفضل نسخة، وبعد ذلك جرى التعديل والتطوير عليها بشكل جماعي من خلال التشاور وتبادل الأفكار بين جميع أعضاء الفريق.

👨‍💻 أعضاء الفريق
علي طفور
يوسف المكبتل
أحمد كحلوس
إبراهيم أحمد
محمد شاوي

هذا الأسلوب ساعد على:
رفع جودة المشروع
تبادل الخبرات بين الطلاب
الوصول إلى حل متكامل ومدروس

> 

---

## ▶️ كيفية تشغيل المشروع

### المتطلبات:
- JDK 17 
- Maven
- IntelliJ IDEA

### خطوات التشغيل:
1. فك ضغط المشروع
2. فتح المشروع باستخدام IDE
3. التأكد من إعدادات قاعدة البيانات في ملف `application.properties`

مثال باستخدام H2:
```properties
spring.datasource.url=jdbc:h2:file:./data/appointmentdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
```

4. تشغيل التطبيق عبر:
```bash
mvn spring-boot:run
```
أو تشغيل الكلاس الرئيسي:
```java
AppointmentSystemApplication
```

5. الوصول للتطبيق عبر المتصفح أو Postman:
```
http://localhost:8080
```

---

## 🔗 الـ APIs الموجودة

### 👤 User APIs
- **تسجيل مستخدم جديد**
  - `POST /api/auth/register`

- **تسجيل الدخول**
  - `POST /api/auth/login`

---

### 🛠 Service APIs
- **إضافة خدمة جديدة**
  - `POST /api/services`

- **جلب جميع الخدمات**
  - `GET /api/services`

- **جلب خدمة حسب ID**
  - `GET /api/services/{id}`

---

### 📅 Appointment APIs
- **حجز موعد**
  - `POST /api/appointments`

- **عرض جميع المواعيد**
  - `GET /api/appointments`

- **عرض مواعيد مستخدم معين**
  - `GET /api/appointments/user/{userId}`

- **إلغاء موعد**
  - `DELETE /api/appointments/{id}`

---

## 🧪 اختبار الـ APIs
تم اختبار جميع الـ APIs باستخدام **Postman** مع إرسال واستقبال البيانات بصيغة JSON.

---

## 📎 ملاحظات إضافية
- المشروع قابل للتوسعة وإضافة واجهة Frontend لاحقًا
- يمكن ربطه بتطبيق موبايل (Flutter / Android)
- الكود منظم وفق بنية MVC

---

✍️ **إعداد الطلاب – كلية الهندسة المعلوماتية**

